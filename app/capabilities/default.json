{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. App-defined commands are always allowed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
